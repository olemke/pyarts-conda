name: Build conda packages

on: [push, pull_request]

jobs:
  build:
    name: Build (py${{ matrix.pyversion }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-26-intel", "macos-26"]
        pyversion: ["3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6
      - uses: conda-incubator/setup-miniconda@v4
        with:
          miniforge-version: latest
          channels: conda-forge
          conda-remove-defaults: "true"
          activate-environment: pyarts-build
          environment-file: etc/environment-pyarts-build.yml
      - shell: bash -l {0}
        run: |
          conda info
          conda list
          conda config --show-sources
          conda config --show
          printenv | sort
      - shell: bash -l {0}
        run: rattler-build build --variant python=${{ matrix.pyversion }} -r pyarts
      - uses: actions/upload-artifact@v7
        if: runner.os == 'Linux'
        with:
          name: pyarts-conda-package-${{ matrix.os }}-py${{ matrix.pyversion }}
          path: output/**/pyarts-*.conda
      - uses: actions/upload-artifact@v7
        if: runner.os == 'macOS'
        with:
          name: pyarts-conda-package-${{ matrix.os }}-py${{ matrix.pyversion }}
          path: output/**/pyarts-*.conda
  merge:
      runs-on: ubuntu-latest
      needs: build
      steps:
        - name: Merge Artifacts
          uses: actions/upload-artifact/merge@v7
          with:
            name: pyarts-conda-packages
            retention-days: 7
            delete-merged: true
